apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hope.fullname" . }}-celeryflower
  labels:
    {{- include "hope.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $key, $value :=  .Values.celeryflower.secret }}
  {{ if eq $value "" }}
  {{ else }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end }}
