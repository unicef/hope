---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kobocat-media-pvc
  labels:
    app: {{ include "kobo.labels.app" . }}
    component: kobocat
    chart: {{ include "kobo.labels.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  storageClassName: "{{ .Values.global.storageClass }}"
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
