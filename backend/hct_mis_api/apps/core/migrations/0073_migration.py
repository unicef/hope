# Generated by Django 3.2.20 on 2023-09-01 07:32

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('core', '0072_migration'),
    ]

    operations = [
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE FUNCTION program_cycle_business_area_seq() RETURNS trigger 
                LANGUAGE plpgsql
                AS $$
                BEGIN
                    EXECUTE format('CREATE SEQUENCE IF NOT EXISTS program_cycle_business_area_seq_%s', translate(NEW.id::text, '-', '_'));
                    RETURN NEW;
                END
                $$;
            """,
        ),
        migrations.RunSQL(
            sql="""
            CREATE TRIGGER program_cycle_business_area_seq AFTER INSERT ON core_businessarea FOR EACH ROW EXECUTE PROCEDURE program_cycle_business_area_seq();
            """,
        ),
        migrations.RunSQL(
            sql="""
            CREATE OR REPLACE FUNCTION program_cycle_business_area_for_old_ba(id text)
                RETURNS text 
                LANGUAGE plpgsql
                AS $$
                BEGIN
                    EXECUTE format('CREATE SEQUENCE IF NOT EXISTS program_cycle_business_area_seq_%s', translate(id::text, '-', '_'));
                    RETURN id;
                END;
                $$;
                SELECT id, program_cycle_business_area_for_old_ba(id::text) AS result FROM core_businessarea;
            """,
        )
    ]
